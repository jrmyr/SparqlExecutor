# SparqlExecutor

Execute a SPARQL query against an RDF file in format rdf/xml and see the result.
Even if the functionality is yet VERY BASIC, it is useful when developing or checking SPARQL queries.

The status can be considered as pre-alpha.
